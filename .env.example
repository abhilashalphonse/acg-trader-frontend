# ACG Trader V1 / Vite
# Twelve Data REST + WebSocket feed used by TradingView Lightweight Charts.
# For local development only. Do not commit your real key.
VITE_TWELVE_DATA_API_KEY=YOUR_TWELVE_DATA_API_KEY

# Production note:
# Twelve Data recommends keeping API keys server-side. Before public launch,
# proxy REST/WebSocket access through the ACG backend rather than exposing the
# key in browser code.
